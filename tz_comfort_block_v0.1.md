# ТЗ: Универсальный блок комфорта электровелосипеда (черновик v0.1)

## 1. Назначение
Единый контроллер, объединяющий:
- управление светом (основной свет, ДХО),
- поворотники,
- звуковой сигнал (гудок),
- индикацию режима помощи (PAS) и газа (throttle),
- защитную логику по тормозам,
- вывод информации на светодиодную матрицу 8х32,
- приём команд с рулевых кнопок / джойстика / крутилки яркости.

Проект тестовый, поэтапный. Отключение мотора по тормозу уже реализовано штатным контроллером мотор-колеса — дублировать эту логику не требуется, наша задача только отображение состояния "B" на экране.

PAS-эмуляция реализуется на этом же контроллере (не выносится отдельно): газ читается через АЦП, обрабатывается программно (кривая отклика + смешение с эмулированным PAS-сигналом) и выводится через встроенный ЦАП на штатный контроллер мотора (throttle-by-wire). Тормоз при этом идёт в моторконтроллер напрямую, в обход нашей платы — критичная для безопасности функция не должна зависеть от стабильности нашей прошивки.

**Требование по отказоустойчивости (обязательное, не бэклог):** выход газа по умолчанию (при старте, перезагрузке, зависании) должен быть 0. Watchdog-таймер ESP32 обязателен для перезапуска при зависании основного цикла.

Платформа: **ESP32** (решено — Nano не хватает по прерываниям/ШИМ-каналам/пинам, плюс нужен встроенный ЦАП для throttle-by-wire с минимальной задержкой).

## 2. Этапность
**Этап 1 (текущий):** общий режим — все функции работают одновременно через существующий пульт с фиксированными кнопками (фара, поворотник влево, поворотник вправо, гудок).
**Этап 2 (позже):** режимы день / ночь / аутдор и переключение между ними через отжимные кнопки и/или джойстик.

## 3. Входы (черновой список)
| # | Сигнал | Тип | Примечание |
|---|--------|-----|------------|
| 1 | Кнопка "Фара" | цифровой, фиксированная | вкл/выкл основного света |
| 2 | Кнопка "Поворотник влево" | цифровой, фиксированная | |
| 3 | Кнопка "Поворотник вправо" | цифровой, фиксированная | |
| 4 | Кнопка "Гудок" | цифровой, отжимная (momentary) | |
| 5 | Крутилка яркости | аналоговый | управляет ШИМ фары, ДХО, дисплея |
| 6 | Датчик тормоза (перед+зад, сведены в одну пару проводов) | цифровой, сухой контакт (подтверждено) | читаем как кнопку, INPUT_PULLUP |
| 7 | Газ/throttle | аналоговый (0-5В?) | throttle-by-wire: режем провод, заводим ТОЛЬКО в наш АЦП, на выходе — ЦАП в моторконтроллер |
| 8 | PAS-датчик (штатный, тип КТ) | импульсный | считывание уровня помощи |
| 9 | Датчик скорости (мотор-колесо) | импульсный | под вопросом (см. открытые вопросы) |
| 10 | Джойстик (опционально) | аналог X/Y + кнопка | для будущего меню режимов |

## 4. Выходы (черновой список)
| # | Канал | Тип | Примечание |
|---|-------|-----|------------|
| 1 | Основной свет (фара) | ШИМ, силовой ключ (мощный MOSFET, параллелятся хоть неск. фар) | |
| 2 | ДХО | ШИМ, силовой ключ | |
| 3 | Поворотник левый | вкл/мигание, БЕЗ ШИМ (PNP-ключ, ~200мА) | индикация на дисплее — стрелка `<`, звуковой тик на каждый цикл мигания |
| 4 | Поворотник правый | вкл/мигание, БЕЗ ШИМ (PNP-ключ, ~200мА) | индикация на дисплее — стрелка `>`, звуковой тик на каждый цикл мигания |
| 5 | Гудок | вкл/выкл, БЕЗ ШИМ (PNP-ключ, ~200мА) | |
| 6 | Задний габарит/стоп-сигнал (RGB, существующий) | триггер по тормозу, ШИМ желательно, если не влезет — просто вкл/выкл | сам RGB-блок не трогаем, добавляем только сигнал "тормоз → красный" |
| 7 | Матрица 8х32 | SPI (MAX7219 x4, подтверждено) | |
| 8 | Эмулятор PAS → газ | программное смешение (max, не диоды), проценты 0-100% на уровень | |
| 9 | (бэклог) Ограничитель скорости / "сервисный режим" | программно, внутри того же throttle-by-wire тракта | принудительно подаёт минимальный уровень газа, активация — 5х нажатий тормоза подряд |
| 10 | Звуковой пищалка (простой активный буззер) | цифровой вкл/выкл | тик поворотника; подтверждения действий позже |
| 11 | Круиз-контроль (простой, разомкнутый цикл) | throttle-by-wire, целевое значение задаётся джойстиком | та же структура что PAS: количество уровней (до 20) + % на каждый, автораспределение; настройка "последний уровень / фиксированный по умолчанию"; опция "сброс при касании газа"; сохранение в NVS; отменяется мгновенно при нажатии тормоза |
| 12 | (бэклог, в самый конец) Круиз-контроль с обратной связью по скорости | замкнутый цикл: датчик скорости → корректировка ЦАП | нормальный "умный" круиз, штатный круиз в моторконтроллере отключить |
| 13 | Сглаживание разгона/торможения газа | программно, throttle-by-wire | РЕАЛИЗОВАНО: гибрид — экспоненциальный фильтр по реальному времени (не по циклам) + доводка до цели при приближении. Раздельные параметры разгон/торможение, слайдеры 0-99, по умолчанию выключено. Настройка — /settings/throttle |

## 5. Логика отображения на матрице
- Активен PAS → крупная буква **P** + уровень помощи (1–7).
- Активен газ → буква **T**.
- Нажат тормоз → буква **B**, приоритет над P и T.
- Индикация текущего состояния света (вкл/выкл, ДХО).
- Скорость — если удастся считать с датчика мотор-колеса.

## 6. Логика безопасности
- Отключение мотора по тормозу уже реализовано штатным контроллером мотор-колеса — нашей логикой не дублируется.
- На экране при нажатом тормозе буква "B" имеет приоритет отображения над "P"/"T".
- Круиз-контроль отменяется мгновенно при нажатии тормоза (та же логика приоритета в throttle-тракте).

## 7. Хранение данных (NVS/flash ESP32)
- Общий пробег этого колеса — хранится постоянно, переживает перезагрузки/отключение питания, сбрасывается ТОЛЬКО вручную отдельной кнопкой/действием.
- Остальное (средняя скорость, история поездок и т.п.) — относится к бортовому компьютеру, решим при реализации этого пункта (низкий приоритет).

## 8. Приоритеты реализации
1. **Высокий, делаем первым:** OTA-обновление прошивки по WiFi.
2. **Средний:** простой круиз-контроль (разомкнутый цикл, регулировка джойстиком).
3. **Низкий, в конце:** бортовой компьютер (пробег/средняя скорость/др.), круиз-контроль с обратной связью по скорости, "сервисный режим" ограничения скорости.
4. **TODO без даты:** вольтметр батареи — на ручке газа уже есть индикация, но на моторконтроллере вроде тоже есть разъём под это — проверить при случае, не обязательно.
5. **TODO без даты:** второй джойстик под правую руку — для быстрого управления поворотниками и т.п. отдельно от основного (левого) джойстика меню/круиза.

## 9. Открытые вопросы
См. отдельный список ниже в чате — требуют ответа перед проектированием схемы.
